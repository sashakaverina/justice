<%= simple_form_for @user, url: share_path do |f| %>
  <%= f.input :email, label: "Email address:" %>
  <%= f.input :password, label: "Passcode:" %>
  <%= f.submit "Share now", class: 'button-small box-shadow w-100', id: 'show-bottom' %>
<% end %>
